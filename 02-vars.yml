- name: Demo On Variables
  hosts: all
  vars:
    URL: play.example.com
  tasks:
    - name: Print Varibale
      debug:
        msg: URL = {{ URL }}
  vars:
    URL: ansible.example.com
  tasks:
    - name: print from tasks
      debug:
        msg: URL = {{ URL }}